document.addEventListener("DOMContentLoaded",function(){console.log("Flaha website loaded");const currentLocation=window.location.pathname;const navLinks=document.querySelectorAll("nav ul li a");navLinks.forEach(link=>{const linkPath=link.getAttribute("href");if(currentLocation===linkPath||linkPath!=="/"&&currentLocation.startsWith(linkPath)){link.classList.add("active")}});const mobileMenuToggle=document.querySelector(".mobile-menu-toggle");if(mobileMenuToggle){const navMenu=document.querySelector("nav ul");mobileMenuToggle.addEventListener("click",function(){navMenu.classList.toggle("show")})}});