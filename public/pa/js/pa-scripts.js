document.addEventListener("DOMContentLoaded",function(){console.log("PA section loaded");const filterButtons=document.querySelectorAll(".tool-filter button");if(filterButtons.length>0){const tools=document.querySelectorAll(".tool");filterButtons.forEach(button=>{button.addEventListener("click",function(){const category=this.getAttribute("data-category");filterButtons.forEach(btn=>btn.classList.remove("active"));this.classList.add("active");tools.forEach(tool=>{if(category==="all"||tool.getAttribute("data-category")===category){tool.style.display="block"}else{tool.style.display="none"}})})})}});